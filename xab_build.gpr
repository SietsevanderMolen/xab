with "xcbada";

project xab_build is
   Version := "1";

   for Source_Dirs use ("src/**");
   for Object_Dir use "build";
   for Exec_Dir use "exe";

   package Ide is
      for Documentation_Dir use "doc";
   end Ide;

   for Library_Name use "xab";
   for Library_Dir use "lib";

end xab_build;
